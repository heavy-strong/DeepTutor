{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splash",
  "description": "The bundled boot screen: watches runtime status and can retry / open logs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
