{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "deeptutor-web",
  "description": "The DeepTutor Web UI served by the local runtime. Only what web/components/desktop/DesktopBridge.tsx needs: opening external links in the system browser.",
  "windows": ["main"],
  "remote": {
    "urls": [
      "http://localhost:*/*",
      "http://127.0.0.1:*/*"
    ]
  },
  "permissions": [
    "core:event:default",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
